[package]
name = "qomm-dsl"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "A small total language for market-maker price rules, from which the circuit, the bit width and the audit obligations are all derived."

[dependencies]
sha2.workspace = true
